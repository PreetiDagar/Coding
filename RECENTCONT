#include <bits/stdc++.h>
using namespace std;

int main() {
	int n,c,a,b  ;
	string var;
	cin>>n;
	for(int i=0;i<n;i++)
	{
	    cin>>c; 
	    a=0;
	    b=0;
	    while(c--)
	    {
	        cin>>var;
	        if(var=="START38")
	        {a=a+1;}
	        else{b=b+1;}
	    }
	    cout<<a<<" "<<b<<endl;
	    
	    
	}
	return 0;
}
